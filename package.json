{
  "name": "cluadflare-worker-waitlist",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": " bun run --bun astro dev",
    "build": "bun run --bun astro build",
    "preview": "bun run --bun astro preview",
    "astro": "bun run --bun astro"
  },
  "dependencies": {
    "astro": "^6.3.5"
  }
}
